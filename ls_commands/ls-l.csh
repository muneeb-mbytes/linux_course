#ls -l command list the content of directory in long list format including owner of the content,size of the content, last modified date of the content, file or directory name.
echo "${RED} list content of directory  in a long list format ${NC}"
ls -l
