#This is cd / command
#Used to go back to Root directory

cd /

# We are using single-quotes as delimiters
set expected_command = 'cd "dir name"'

echo "print expected_command = $expected_command"

if ("$mainmenuinput" == "$expected_command") then
