# This commands used to delete the directories which
#  starts with a particular prefix .
#
#  syntax : rm -- -prefix
#
 rm -- -mani

# this will deletes all the directories whose names 
# starts with mani(like manipal manipal_b6 and so on).
