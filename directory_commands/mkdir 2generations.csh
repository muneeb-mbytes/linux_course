# This command is used for creating a 2 parent dir's with child dir's
#
# syntax : mkdir -p parent1 parent2/child1
#
mkdir -p linux_basics github/merging

# in your present directory linux_basics and github directories were
# created which github has a child directory named merging

echo " ${YELLOW} Two parent directories named linux_basics and github\ndirectories were created in which github has a child dir named merging ${NC}"

