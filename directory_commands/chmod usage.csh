# This command is used for giving permissions to an already existing directory
#
# syntax : chmod 777 directoryname
#
chmod 777 linux_basics

# the directory named linux_basics is can be accessable for reading,
#  writing and executable by anyone .
echo "${YELLOW}linux_basics (existing)directory can be accessable for reading,\nwriting and executable by anyone${NC}"
